---
keywords: [advanced, bitcoin, btc, tutorial, sign transactions]
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";

# Firmar transacciones

<MarkdownChipRow labels={["Avanzado", "Bitcoin", "Tutorial"]} />

Antes de que una transacción pueda enviarse a la red de Bitcoin, cada entrada
debe estar firmada.

## ECDSA de umbral

Los canisters pueden firmar transacciones con ECDSA de umbral mediante el método
`sign_with_ecdsa`.

:::info

Para probar canisters localmente que utilicen los siguientes fragmentos de
código, es necesario habilitar el desarrollo local de Bitcoin. Para hacerlo,
puedes iniciar la réplica local con `dfx start --enable-bitcoin` o incluir la
siguiente configuración en el archivo `dfx.json` del proyecto:

```json reference
https://github.com/dfinity/examples/blob/master/motoko/basic_bitcoin/dfx.json#L9-L16
```

[Más información sobre las opciones de configuración de `dfx.json`](https://internetcomputer.org/docs/building-apps/developer-tools/dfx-json-reference).

:::

El siguiente fragmento muestra un ejemplo simplificado de cómo firmar una
transacción de Bitcoin en el caso especial donde todas las entradas hacen
referencia a `outpoints` que pertenecen a `own_address`, y `own_address` es una
dirección `P2PKH`.

<Tabs groupId="languages">
<TabItem value="motoko" label="Motoko" default>

```motoko no-repl reference
https://github.com/dfinity/examples/blob/c4cbbfc72c4262c7aad34268ac48e85f3851b40b/motoko/basic_bitcoin/src/basic_bitcoin/src/BitcoinWallet.mo#L161-L212
```

</TabItem>
<TabItem value="rust" label="Rust">

```rust reference
https://github.com/dfinity/examples/blob/c4cbbfc72c4262c7aad34268ac48e85f3851b40b/rust/basic_bitcoin/src/basic_bitcoin/src/bitcoin_wallet/p2pkh.rs#L165-L217
```

</TabItem>
</Tabs>

## Schnorr de umbral

Los canisters pueden firmar transacciones con Schnorr de umbral mediante el
método `sign_with_schnorr`.

### Firmar transacciones P2TR con clave de ruta

El siguiente fragmento muestra un ejemplo simplificado de cómo firmar una
transacción de Bitcoin en el caso especial donde todas las entradas hacen
referencia a `outpoints` que pertenecen a `own_address`, y `own_address` es una
dirección `P2TR`.

<Tabs groupId="languages">
<TabItem value="rust" label="Rust" default>

```rust reference
https://github.com/dfinity/examples/blob/2e748ec113f3c829c076fd3733264aa0ab9e5b6b/motoko/basic_bitcoin/src/basic_bitcoin/src/P2tr.mo#L152-L185
```

</TabItem>
</Tabs>

### Firmar transacciones P2TR con script de ruta

El siguiente fragmento muestra un ejemplo simplificado de cómo firmar una
transacción de Bitcoin en el caso especial donde todas las entradas hacen
referencia a `outpoints` que pertenecen a `own_address`, y `own_address` es una
dirección `P2TR` con script de ruta.

<Tabs groupId="languages">
<TabItem value="rust" label="Rust" default>

```rust reference
https://github.com/dfinity/examples/blob/8ccaf6ce9fbd2d39e44b7bbc7f339606e1ac1233/rust/basic_bitcoin/src/basic_bitcoin/src/bitcoin_wallet/p2tr.rs#L357-L411
```

</TabItem>
</Tabs>

## Más información

[Más información sobre ECDSA de umbral](/docs/building-apps/network-features/signatures/t-ecdsa).

[Más información sobre Schnorr de umbral](/docs/building-apps/network-features/signatures/t-schnorr).
