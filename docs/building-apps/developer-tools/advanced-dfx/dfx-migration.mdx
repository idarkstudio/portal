---
keywords:
  [
    advanced,
    dfx,
    migration guide,
    v0.17.0,
    use dfxvm to update dfx,
    update dfx,
    dfxvm,
  ]
---

import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";

# dfx v0.17.0 CI guia de migración

<MarkdownChipRow labels={["Avanzado", "dfx workflow"]} />

`dfxvm` es una nueva herramienta utilizada para instalar y gestionar diferentes
versiones de `dfx`. En las versiones 0.17.0 y posteriores de `dfx`, se utiliza
`dfxvm` para instalar y actualizar `dfx`. Esto significa que los trabajos de CI
que instalan `dfx` deberán actualizar su flujo de trabajo.

## Acciones de GitHub

Si tu trabajo de CI utiliza acciones de GitHub, puedes editar tu trabajo de
instalación de `dfx` para utilizar la siguiente configuración:

```
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Install dfx
      uses: dfinity/setup-dfx@main
    - name: Confirm successful installation
      run: dfx --version
```

## Trabajo CI Manual

Si usas un trabajo CI manual, necesitarás seguir estos pasos:

1. Establece `DFXVM_INIT_YES=true` para que el instalador omita los mensajes de
   confirmación:

```
DFXVM_INIT_YES=true sh -ci "$(curl -fsSL https://sdk.dfinity.org/install.sh)"
```

2. Agrega la ruta para `dfxvm bin` a tu variable PATH:

```
# Linux:
"$HOME/.local/share/dfx/bin"

# macOS:
"$HOME/Library/Application Support/org.dfinity.dfx/bin"
```

3. Dependiendo de la configuración de tu script CI, es posible que necesites
   cargar el script `dfx/env` también:

```
# Linux:
source "$HOME/.local/share/dfx/env"

# macOS:
source "$HOME/Library/Application Support/org.dfinity.dfx/env"
```

## Docker

Para trabajos de CI que utilicen entornos Docker, se puede utilizar la siguiente
configuración de Dockerfile:

```
ARG DFX_VERSION
RUN DFXVM_INIT_YES=true sh -ci "$(curl -fsSL https://sdk.dfinity.org/install.sh)"
RUN . $HOME/.local/share/dfx/env
ENV PATH="/root/.local/share/dfx/bin:$PATH"
```

## Resources

- [`dfxvm` documentation](/docs/building-apps/developer-tools/dfxvm/dfxvm-install).

- [`dfxvm` GitHub repo](https://github.com/dfinity/dfxvm).

- [`dfx` documentation](/docs/building-apps/developer-tools/dfx/).

- [IC SDK GitHub repo](https://github.com/dfinity/sdk).

## Recursos

- [`dfxvm` documentación](/docs/building-apps/developer-tools/dfxvm/dfxvm-install).

- [`dfxvm` repositorio de GitHub](https://github.com/dfinity/dfxvm).

- [`dfx` documentación](/docs/building-apps/developer-tools/dfx/).

- [IC SDK respositorio de GitHub](https://github.com/dfinity/sdk).
