import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";

# dfxvm update

<MarkdownChipRow labels={["Referencia"]} />

Revisa la última versión de dfx, la instala si aún no está instalada y la
establece como la versión predeterminada.

## Uso

```bash
dfxvm update
```
