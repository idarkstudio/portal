---
keywords: [reference, dfx, dfx.json schema]
---

import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";

# dfx.json schema

<MarkdownChipRow labels={["dfx", "Referencia"]} />

Esta es la referencia del esquema para `dfx.json` en formato
[JSON Schema](https://json-schema.org).

Para descargar el esquema, guarde [este archivo](./dfx/dfx-json-schema.json).

Para generar el esquema en un archivo usted mismo, use
`dfx schema --outfile path/to/outfile.json` como se describe en la referencia de
[`dfx schema`](/docs/building-apps/developer-tools/dfx/).

```json file=./dfx/dfx-json-schema.json

```
